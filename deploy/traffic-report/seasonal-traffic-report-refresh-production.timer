[Unit]
Description=Check for public traffic report source changes every five minutes

[Timer]
# The runner compares source and projection watermarks before doing any refresh.
OnCalendar=*:0/5
AccuracySec=1s
Persistent=true
Unit=seasonal-traffic-report-refresh.service

[Install]
WantedBy=timers.target
